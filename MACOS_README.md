# 视频音频高速下载神器 - macOS 版本

## 🍎 macOS 打包指南

### 方法一：使用打包脚本（推荐）

1. **在 macOS 终端中**，进入项目目录：
   ```bash
   cd /path/to/ytb-dl-webui
   ```

2. **运行打包脚本**：
   ```bash
   chmod +x build_macos.sh
   ./build_macos.sh
   ```

3. **完成后**，应用程序位于 `dist/视频下载神器.app`

---

### 方法二：手动打包

1. **安装 Python 3.9+**（如果未安装）：
   ```bash
   # 使用 Homebrew
   brew install python@3.11
   brew install python-tk@3.11
   ```

2. **创建虚拟环境并安装依赖**：
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install pyinstaller yt-dlp requests brotli websockets mutagen pycryptodomex
   ```

3. **使用 PyInstaller 打包**：
   ```bash
   pyinstaller macos_app.spec
   ```

4. **找到生成的应用**：
   ```
   dist/视频下载神器.app
   ```

---

### 方法三：直接运行 Python 脚本

如果不需要打包成 .app，可以直接运行：

```bash
# 安装依赖
pip3 install yt-dlp requests

# 运行程序
python3 tkinter_app_macos.py
```

---

## 📦 安装和使用

### 安装
1. 将 `视频下载神器.app` 拖到 **应用程序** 文件夹

### 首次运行
macOS 可能会阻止未签名的应用运行。解决方法：

1. 右键点击 `视频下载神器.app`
2. 选择 **打开**
3. 在弹出的对话框中点击 **打开**

或者使用终端命令：
```bash
xattr -cr /Applications/视频下载神器.app
```

### 使用
1. 打开应用
2. 粘贴视频链接（支持 YouTube、Bilibili、TikTok、Instagram 等）
3. 选择格式
4. 点击 **开始下载**
5. 下载的文件保存在 `~/Downloads/视频下载/` 目录

---

## 🔧 常见问题

### Q: 提示「无法打开，因为无法验证开发者」
**A:** 右键点击应用 → 选择「打开」→ 确认打开

### Q: YouTube 下载失败
**A:** YouTube 可能需要登录验证。请先在 Safari 或 Chrome 中登录 YouTube 账号。

### Q: 没有声音
**A:** 需要安装 FFmpeg：
```bash
brew install ffmpeg
```

### Q: tkinter 报错
**A:** 确保安装了 python-tk：
```bash
brew install python-tk@3.11
```

---

## 📋 支持的平台

| 平台 | 支持 |
|------|------|
| YouTube | ✅ |
| YouTube Shorts | ✅ |
| Bilibili | ✅ |
| TikTok | ✅ |
| Instagram | ✅ |
| Suno | ✅ |
| Sora | ✅ |

---

## 🛠 开发者

如需修改代码，主要文件：
- `tkinter_app_macos.py` - macOS 兼容的 GUI 主程序
- `macos_app.spec` - PyInstaller 打包配置
- `build_macos.sh` - 自动打包脚本

---

版本: 5.1.0
